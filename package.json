{
  "name": "deistask",
  "version": "1.0.0",
  "description": "VSTS task for Deis",
  "main": "index.js",
  "scripts": {
    "pretest": "tsc -p .",
    "predeploy": "tsc -p .",
    "deploy": "deploy.bat",
    "build": "tfx extension create --manifest-globs vss-extension.json",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Tsuyoshi Ushio",
  "license": "ISC",
  "dependencies": {
    "chai": "^4.0.2",
    "mocha": "^3.4.2",
    "mocha-junit-reporter": "^1.13.0",
    "path": "^0.12.7",
    "vsts-task-lib": "^2.0.5"
  }
}
